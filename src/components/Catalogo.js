import React, { PureComponent } from 'react';

export default class Catalogo extends PureComponent {
    render() {
        return (
            <div>

            </div>
        );
    }
}
